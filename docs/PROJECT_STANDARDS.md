# Digital Wardrobe 项目开发规范

> **版本**: 1.0.0  
> **最后更新**: 2026-02-12  
> **适用范围**: 所有项目贡献者

---

## 📋 目录

1. [项目架构](#项目架构)
2. [详细规范](#详细规范)
3. [质量红线](#质量红线)
4. [文档规范](#文档规范)

---

## 🏗️ 项目架构

### 全局目录结构

```
digital-wardrobe/
├── .agent/                    # 全局 Agent 配置
│   ├── rules/                 # 详细开发规则
│   └── skills/                # 常用技能脚本
├── docs/                      # 全局项目文档
├── wardrobe-front/            # 前端项目 (Digital Wardrobe)
├── wardrobe-backend/
│   ├── ai-service/            # AI 服务 (Python)
│   └── java-service/          # Java 服务 (Spring Boot)
└── openspec/                  # 规范文件
```

### 技术栈概览

| 子项目 | 语言 | 框架 | 质量工具 |
| :--- | :--- | :--- | :--- |
| `wardrobe-front` | TypeScript 5.8 | React 19 + Vite 6 | ESLint, Vitest |
| `ai-service` | Python 3.10+ | FastAPI | Ruff, MyPy, Pytest |
| `java-service` | Java 17/21 | Spring Boot | — |

### 前端目录结构

```
wardrobe-front/
├── .agent/                    # 前端 Agent 配置
├── supabase/                  # 数据库相关
├── src/                       # 源代码目录
│   ├── features/              # 功能模块 (Feature-based)
│   ├── components/            # 通用组件
│   ├── hooks/                 # 全局 Hooks
│   ├── services/              # 全局服务
│   ├── types/                 # 全局类型
│   └── utils/                 # 工具函数
└── ...
```

### 通用架构原则

1. **分层架构**: `API → Service → Model`，禁止跨层直接调用。
2. **单一职责 (SRP)**: 每个模块/类/函数只负责一个功能。
3. **配置驱动**: 禁止硬编码敏感信息，使用 `.env` 管理。
4. **类型安全**: TypeScript `strict: true`，Python `mypy --strict`。
5. **关注点分离**: UI、业务逻辑、数据层严格分离。

### 跨项目协作

- **接口契约**: AI 服务 `POST /generate-outfit`，前端通过 `VITE_AI_SERVICE_URL` 引用。
- **降级策略**: AI 服务不可用时，前端自动回退到本地规则引擎。
- **Git 分支**: `main` (稳定) / `feature/*` (开发) / `fix/*` (修复)

---

## 📝 详细规范

为了保持本文档的简洁性，详细的开发规范已拆分为独立的规则文件，存储在 `.agent/rules/` 目录下。Agent 会根据上下文自动加载这些规则。

### 1. 代码风格与命名
请参考: [Coding Style Guidelines](../.agent/rules/coding-style.md)
- 命名规范 (文件、变量、函数)
- 代码格式化 (Prettier)
- 注释规范

### 2. TypeScript 最佳实践
请参考: [TypeScript Best Practices](../.agent/rules/typescript-best-practices.md)
- 类型定义 (Interface vs Type)
- 严格模式设置
- 类型安全 (避免 any, 使用 Type Guards)

### 3. React 开发规范
请参考: [React Best Practices](../.agent/rules/react-best-practices.md)
- 组件结构与Props设计
- Hooks 使用规范
- 状态管理 (Local vs Global)
- 性能优化 (Memoization)



### 4. UI 与样式
请参考: [UI & Styling Guidelines](../.agent/rules/ui-styling.md)
- CSS 架构与变量
- 组件样式方案 (CSS Modules/Tailwind)

### 5. 测试规范
请参考: [Testing Standards](../.agent/rules/testing-standards.md)
- 单元测试 (Vitest)
- 组件测试 (Testing Library)
- 测试策略与覆盖率

### 6. Git 工作流
请参考: [Git Workflow](../.agent/rules/git-workflow.md)
- Conventional Commits
- 分支管理策略
- Pull Request 规范

---

## 🚦 质量红线 (Go/No-Go)

| 检查项 | 标准 | 级别 |
| :--- | :--- | :--- |
| 类型检查 | TS `strict` / MyPy `strict` 零错误 | 🔴 阻断 |
| 单元测试 | 核心 Service 层必须覆盖 | 🔴 阻断 |
| 敏感信息 | `.env` 禁止提交 Git | 🔴 阻断 |
| 数据隔离 | 查询必须带 `user_id` 过滤 | 🔴 阻断 |
| 代码风格 | ESLint / Ruff 零 warning | 🟡 警告 |

---

## 📚 文档规范

- **README.md**: 每个项目必须包含标准的 README。
- **注释**: 解释"为什么"而不是"是什么"，复杂逻辑必须有 JSDoc/Docstring。
- **更新维护**: 代码变更时同步更新相关文档。


